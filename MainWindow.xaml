<Window x:Class="CMLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMLauncher"
        mc:Ignorable="d"
        Title="CM Launcher" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#242424">
    <Window.Resources>
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E3E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E3E"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="PlayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="80,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E8E41"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InstanceComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#303030"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#505050"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Top account info panel -->
        <Border Grid.Column="0" Grid.Row="0" Background="#222222" BorderBrush="#333333" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="10,5">
                <Ellipse Width="30" Height="30" Fill="#555555" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="Username" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                    <TextBlock Text="Account" Foreground="#AAAAAA" FontSize="11"/>
                </StackPanel>
                <Path Data="M 0,0 L 5,5 L 10,0" Stroke="White" StrokeThickness="1" 
                      Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Top header with tabs -->
        <Grid Grid.Column="1" Grid.Row="0" Background="#222222">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Edition title -->
            <TextBlock Grid.Row="0" Text="CASTLEMINER Z" Foreground="White" 
                       FontWeight="Bold" Margin="20,5,0,0" FontSize="12"/>
            
            <!-- Tab buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Content="Play" Style="{StaticResource TabButtonStyle}" Tag="Play" Click="TabButton_Click" 
                        BorderThickness="0,0,0,2" BorderBrush="#4CAF50"/>
                <Button Content="Installations" Style="{StaticResource TabButtonStyle}" Tag="Installations" Click="TabButton_Click"/>
                <Button Content="Patch Notes" Style="{StaticResource TabButtonStyle}" Tag="PatchNotes" Click="TabButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Left sidebar -->
        <Border Grid.Column="0" Grid.Row="1" Background="#252525">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Sidebar navigation -->
                <StackPanel Grid.Row="0" Margin="0,5,0,0">
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="Home">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⌂" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="HOME" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="CMZ" 
                            Background="#333333" Foreground="White">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="◆" FontSize="14" Margin="0,0,10,0"/>
                            <StackPanel>
                                <TextBlock Text="CASTLEMINER Z" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="CMW">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="◆" FontSize="14" Margin="0,0,10,0"/>
                            <StackPanel>
                                <TextBlock Text="CM WARFARE" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>

                    <!-- Other game versions could be added here -->
                </StackPanel>

                <!-- Bottom sidebar items -->
                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="Changelog">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="≡" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="WHAT'S NEW" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="Settings">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="SETTINGS" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    
                    <TextBlock Text="v1.0.0" FontSize="11" Foreground="#888888" Margin="15,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content area -->
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Content frame -->
            <Frame x:Name="MainContentFrame" Grid.Row="0" NavigationUIVisibility="Hidden" Background="#202020"/>

            <!-- Bottom panel with version selection and play button -->
            <Grid Grid.Row="1" Background="#252525">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Version selector dropdown -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="10,15">
                    <TextBlock Text="▼" VerticalAlignment="Center" Foreground="#AAAAAA" Margin="0,0,5,0"/>
                    <StackPanel>
                        <TextBlock x:Name="VersionNumberTextBlock" Text="1.21.5-forge-55.0.23" Foreground="White" FontSize="12"/>
                    </StackPanel>
                </StackPanel>

                <!-- Additional info could go here -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0">
                    <!-- Can add content like "Included with Xbox Game Pass" here -->
                </StackPanel>
                
                <!-- Play button -->
                <Button Grid.Column="2" Content="PLAY" Style="{StaticResource PlayButtonStyle}" 
                        Click="PlayButton_Click" Margin="10"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
