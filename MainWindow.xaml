<Window x:Class="CMLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMLauncher"
        mc:Ignorable="d"
        Title="CM Launcher" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#242424">
    <Window.Resources>
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E3E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sidebar button with explicit selected-state styling via attached property -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="buttonBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover visual for any item -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E3E"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <!-- Selected state visual via attached property -->
                            <Trigger Property="local:SelectionProperties.IsSelected" Value="True">
                                <Setter Property="Background" Value="#2F2F2F"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <!-- Hover + Selected: slightly brighter than selected -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="local:SelectionProperties.IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="#444444"/>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiTrigger>
                            <!-- Pressed visual -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="PlayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="80,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E8E41"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InstanceComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#303030"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#505050"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- Flat toggle look for the bottom-left selector -->
        <Style x:Key="InstallToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#2D2D2D"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#303030"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Menu item style inside popup -->
        <Style x:Key="InstallMenuItemStyle" TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Selected visual: Minecraft green -->
                            <Trigger Property="local:SelectionProperties.IsSelected" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#4CAF50"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <!-- Hover when selected: slightly brighter -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="local:SelectionProperties.IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="bg" Property="Background" Value="#5BCB60"/>
                            </MultiTrigger>
                            <!-- Normal hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#404040"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#4A4A4A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Top account info panel -->
        <Border Grid.Column="0" Grid.Row="0" Background="#222222" BorderBrush="#333333" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="10,5">
                <Ellipse Width="30" Height="30" Fill="#555555" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="Username" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                    <TextBlock Text="Account" Foreground="#AAAAAA" FontSize="11"/>
                </StackPanel>
                <Path Data="M 0,0 L 5,5 L 10,0" Stroke="White" StrokeThickness="1" 
                      Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Top header with tabs -->
        <Grid Grid.Column="1" Grid.Row="0" Background="#222222">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Edition title -->
            <TextBlock Grid.Row="0" Text="CASTLEMINER Z" Foreground="White" 
                       FontWeight="Bold" Margin="20,5,0,0" FontSize="12"
                       x:Name="EditionTitleText"/>
            
            <!-- Tab buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" x:Name="TabsPanel">
                <Button Content="Play" Style="{StaticResource TabButtonStyle}" Tag="Play" Click="TabButton_Click" 
                        BorderThickness="0,0,0,2" BorderBrush="#4CAF50"/>
                <Button Content="Installations" Style="{StaticResource TabButtonStyle}" Tag="Installations" Click="TabButton_Click"/>
                <Button Content="Patch Notes" Style="{StaticResource TabButtonStyle}" Tag="PatchNotes" Click="TabButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Left sidebar -->
        <Border Grid.Column="0" Grid.Row="1" Background="#252525">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Sidebar navigation -->
                <StackPanel Grid.Row="0" Margin="0,5,0,0" x:Name="SidebarPanel">
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="CMZ" Name="btnCMZ">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="◆" FontSize="14" Margin="0,0,10,0"/>
                            <StackPanel>
                                <TextBlock Text="CASTLEMINER Z" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="CMW" Name="btnCMW">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="◆" FontSize="14" Margin="0,0,10,0"/>
                            <StackPanel>
                                <TextBlock Text="CM WARFARE" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Bottom sidebar items -->
                <StackPanel Grid.Row="1" Margin="0,0,0,10" x:Name="BottomSidebarPanel">
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="Changelog" Name="btnChangelog">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="≡" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="WHAT'S NEW" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="Settings" Name="btnSettings">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="SETTINGS" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    
                    <TextBlock Text="v1.0.0" FontSize="11" Foreground="#888888" Margin="15,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content area -->
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Content frame -->
            <Frame x:Name="MainContentFrame" Grid.Row="0" NavigationUIVisibility="Hidden" Background="#202020"/>

            <!-- Bottom panel with version selection and play button -->
            <Grid Grid.Row="1" Background="#252525">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Installation selector (bottom-left) -->
                <Grid Grid.Column="0" VerticalAlignment="Center" Margin="10,8,0,8">
                    <ToggleButton x:Name="InstallToggle" Style="{StaticResource InstallToggleStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="28" Height="20" Background="#3A3A3A" CornerRadius="3" VerticalAlignment="Center">
                                <TextBlock Text="⚒" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="8,0" VerticalAlignment="Center">
                                <TextBlock x:Name="SelectedInstallName" Text="forge" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                                <TextBlock x:Name="SelectedInstallVersion" Text="1.21.5-forge-55.0.23" Foreground="#CCCCCC" FontSize="11"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="▾" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </Grid>
                    </ToggleButton>

                    <!-- Popup above; remove padding so width equals content (min = toggle width) and edges align -->
                    <Popup x:Name="InstallPopup"
                           PlacementTarget="{Binding ElementName=InstallToggle}"
                           Placement="Top"
                           VerticalOffset="-4"
                           StaysOpen="False"
                           AllowsTransparency="True"
                           PopupAnimation="Fade"
                           IsOpen="{Binding IsChecked, ElementName=InstallToggle, Mode=TwoWay}">
                        <Border Background="#242424" BorderThickness="0" CornerRadius="4" Padding="0">
                            <StackPanel x:Name="InstallItemsPanel" MinWidth="{Binding ElementName=InstallToggle, Path=ActualWidth}">
                                <!-- Current/Forge selection -->
                                <Button Style="{StaticResource InstallMenuItemStyle}" Click="SelectInstallation_Click" Tag="forge|1.21.5-forge-55.0.23">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="28" Height="20" Background="#4CAF50" CornerRadius="3"/>
                                        <StackPanel Grid.Column="1" Margin="8,0">
                                            <TextBlock Text="forge" FontWeight="SemiBold"/>
                                            <TextBlock Text="1.21.5-forge-55.0.23" Foreground="#CFCFCF" FontSize="12"/>
                                        </StackPanel>
                                    </Grid>
                                </Button>

                                <Separator Margin="0,6" Background="#404040" Height="1"/>

                                <!-- Latest release -->
                                <Button Style="{StaticResource InstallMenuItemStyle}" Click="SelectInstallation_Click" Tag="Latest release|1.21.8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="28" Height="20" Background="#6A9E3A" CornerRadius="3"/>
                                        <StackPanel Grid.Column="1" Margin="8,0">
                                            <TextBlock Text="Latest release" FontWeight="SemiBold"/>
                                            <TextBlock Text="1.21.8" Foreground="#CFCFCF" FontSize="12"/>
                                        </StackPanel>
                                    </Grid>
                                </Button>

                                <!-- Latest snapshot -->
                                <Button Style="{StaticResource InstallMenuItemStyle}" Click="SelectInstallation_Click" Tag="Latest snapshot|25w34b">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="28" Height="20" Background="#8D6E63" CornerRadius="3"/>
                                        <StackPanel Grid.Column="1" Margin="8,0">
                                            <TextBlock Text="Latest snapshot" FontWeight="SemiBold"/>
                                            <TextBlock Text="25w34b" Foreground="#CFCFCF" FontSize="12"/>
                                        </StackPanel>
                                    </Grid>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Popup>
                </Grid>

                <!-- Additional info could go here -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0"/>
                
                <!-- Play button -->
                <Button Grid.Column="2" Content="PLAY" Style="{StaticResource PlayButtonStyle}" 
                        Click="PlayButton_Click" Margin="10"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
