<Window x:Class="CMLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMLauncher"
        mc:Ignorable="d"
        Title="CM Launcher" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#202020">
    <Window.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E3E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="PlayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="40,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E8E41"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InstanceComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#303030"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#505050"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left sidebar -->
        <Border Grid.Column="0" Background="#252525" BorderBrush="#353535" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo/Header -->
                <TextBlock Text="CM Launcher" 
                           Foreground="White" 
                           FontSize="18" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="15,0,0,0"/>

                <!-- Sidebar navigation -->
                <StackPanel Grid.Row="1" Margin="0,20,0,0">
                    <Button Content="CMZ" Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="CMZ"/>
                    <Button Content="CMW" Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="CMW"/>
                    <Button Content="Settings" Style="{StaticResource SidebarButtonStyle}" Click="SidebarButton_Click" Tag="Settings"/>
                </StackPanel>

                <!-- Instance selection -->
                <StackPanel Grid.Row="2" Margin="15,0,15,15">
                    <TextBlock Text="Instance" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <ComboBox x:Name="InstanceComboBox" 
                              Style="{StaticResource InstanceComboBoxStyle}"
                              SelectionChanged="InstanceComboBox_SelectionChanged">
                        <ComboBoxItem Content="Default"/>
                        <ComboBoxItem Content="Instance 1"/>
                        <ComboBoxItem Content="Instance 2"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Content frame -->
            <Frame x:Name="MainContentFrame" Grid.Row="0" NavigationUIVisibility="Hidden" Margin="20"/>

            <!-- Bottom panel with play button -->
            <Border Grid.Row="1" Background="#252525" Padding="20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock x:Name="SelectedModTextBlock" 
                                   Text="Selected: CMZ" 
                                   Foreground="White" 
                                   FontSize="16"/>
                        <TextBlock x:Name="VersionTextBlock" 
                                   Text="Version: 1.0.0" 
                                   Foreground="#AAAAAA" 
                                   FontSize="12"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1" 
                            Content="PLAY" 
                            Style="{StaticResource PlayButtonStyle}"
                            Click="PlayButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
